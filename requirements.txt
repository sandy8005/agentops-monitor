# AgentOps Monitor — runtime dependencies, pinned to known-working versions.
# Fresh clone (runtime only):
#   python -m venv venv && pip install -r requirements.txt
# For tests/development, also install:
#   pip install -r requirements-dev.txt

# --- Web / API ---
fastapi==0.141.1
uvicorn==0.52.0
python-multipart==0.0.32          # FastAPI file uploads (resume upload)
starlette==1.3.1                  # FastAPI's ASGI framework (StaticFiles serving)
itsdangerous==2.2.0               # REQUIRED by Starlette SessionMiddleware (signs the session cookie)
slowapi==0.1.10                   # rate limiting — Limiter/RateLimitExceeded used in api.py

# --- Database ---
psycopg2-binary==2.9.12           # main app DB driver
psycopg[binary]==3.3.5            # psycopg v3 — required by the LangGraph Postgres checkpointer
psycopg-pool==3.3.1               # connection pool (checkpointer dependency)

# --- LLM / Gemini ---
google-genai==2.12.1

# --- LangGraph agent orchestration + Postgres checkpointing ---
langgraph==1.2.11
langgraph-checkpoint==4.2.0
langgraph-checkpoint-postgres==3.1.2

# --- Data / parsing ---
pydantic==2.13.4                  # validation throughout
beautifulsoup4==4.15.0            # scraper + HTML description cleaning
requests==2.34.2                  # Adzuna / Remotive APIs, scraper
pypdf==6.14.2                     # resume PDF reading

# --- Auth ---
bcrypt==5.0.0                     # password hashing

# --- Config ---
python-dotenv==1.2.2              # .env loading